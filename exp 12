import nltk
from nltk import CFG, EarleyChartParser

grammar = CFG.fromstring("""
S -> NP VP
NP -> 'dog'
VP -> 'runs'
""")

parser = EarleyChartParser(grammar)
print(list(parser.parse(['dog', 'runs'])))
